# Same MySQL instance the backend uses - the ML pipeline reads resource_usage/
# metrics history and writes predictions/anomaly_detections/failure_predictions
# directly, no HTTP round-trip through the backend API.
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=cloudai
MYSQL_PASSWORD=cloudai_password
MYSQL_DATABASE=cloud_ai_platform
