# Base URL the frontend calls for the backend API. In Docker Compose this is
# proxied through nginx (see frontend/nginx.conf); in local dev it points
# straight at the backend dev server.
VITE_API_BASE_URL=http://localhost:8000/api/v1
